# 白名单配置
whitelist:
  common: # 公共白名单
    - /actuator/**
    - /internal/job/**
  services:
    # 用户中心服务
    usercenter:
      - /auth/login
      - /auth/refresh
    # 基础数据服务
    base:
      - /attachment/stream/**
    # 定时任务调度服务
    job: []