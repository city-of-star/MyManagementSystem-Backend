<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mms.usercenter.service.auth.mapper.UserPreferenceMapper">

    <select id="selectByUserId" resultType="com.mms.usercenter.common.auth.vo.UserPreferenceVo">
        SELECT
            id,
            user_id,
            pref_key,
            pref_value,
            value_type,
            remark,
            create_by,
            create_time,
            update_by,
            update_time
        FROM system_user_preference
        WHERE deleted = 0
          AND user_id = #{userId}
        ORDER BY update_time DESC, id DESC
    </select>

</mapper>
