<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.mms</groupId>
        <artifactId>mms-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>mms-usercenter-bc</artifactId>
    <packaging>pom</packaging>
    <name>MMS UserCenter BC</name>
    <description>用户中心模块</description>

    <modules>
        <module>mms-usercenter-bc-common</module>
        <module>mms-usercenter-bc-controller</module>
        <module>mms-usercenter-bc-feign-api</module>
        <module>mms-usercenter-bc-server</module>
        <module>mms-usercenter-bc-service</module>
    </modules>

    <!-- 统一依赖，所有子模块都会继承 -->
    <dependencies>

        <!-- 依赖 mms-common-bc-core 模块 -->
        <dependency>
            <groupId>com.mms</groupId>
            <artifactId>mms-common-bc-core</artifactId>
        </dependency>

        <!-- 依赖 mms-common-bc-threadpool 模块 -->
        <dependency>
            <groupId>com.mms</groupId>
            <artifactId>mms-common-bc-threadpool</artifactId>
        </dependency>

        <!-- 依赖 mms-common-bc-datasource 模块 -->
        <dependency>
            <groupId>com.mms</groupId>
            <artifactId>mms-common-bc-datasource</artifactId>
        </dependency>

        <!-- 依赖 mms-common-bc-cache 模块 -->
        <dependency>
            <groupId>com.mms</groupId>
            <artifactId>mms-common-bc-cache</artifactId>
        </dependency>

        <!-- 依赖 mms-common-bc-es 模块 -->
        <dependency>
            <groupId>com.mms</groupId>
            <artifactId>mms-common-bc-es</artifactId>
        </dependency>

        <!-- 依赖 mms-common-bc-mq 模块 -->
        <dependency>
            <groupId>com.mms</groupId>
            <artifactId>mms-common-bc-mq</artifactId>
        </dependency>

        <!-- 依赖 mms-common-bc-security 模块 -->
        <dependency>
            <groupId>com.mms</groupId>
            <artifactId>mms-common-bc-security</artifactId>
        </dependency>

        <!-- 依赖 mms-common-bc-webmvc 模块 -->
        <dependency>
            <groupId>com.mms</groupId>
            <artifactId>mms-common-bc-webmvc</artifactId>
        </dependency>

    </dependencies>

    <!-- 依赖版本管理 -->
    <dependencyManagement>
        <dependencies>

            <!-- UserCenter模块内部依赖管理 -->
            <dependency>
                <groupId>com.mms</groupId>
                <artifactId>mms-usercenter-bc-common</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.mms</groupId>
                <artifactId>mms-usercenter-bc-service</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.mms</groupId>
                <artifactId>mms-usercenter-bc-controller</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.mms</groupId>
                <artifactId>mms-usercenter-bc-feign-api</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Feign模块依赖管理 -->
            <dependency>
                <groupId>com.mms</groupId>
                <artifactId>mms-base-bc-feign-api</artifactId>
                <version>${project.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

</project>